services:
  - type: web
    name: openrailtracker
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install --prefix backend --no-audit --no-fund
    startCommand: node backend/server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
